<%= form_for(@dishes_type) do |f| %>
  <% if @dishes_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dishes_type.errors.count, "error") %> prohibido este Tipo de Plato:</h2>

      <ul>
      <% @dishes_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripción %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
